<view class="list-container">
  <view class="list-wrap">
    <view class="list-item" wx:for="{{bestListDatas}}" wx:for-item="bestList" wx:key="index">
      <image src="{{bestList.goods_thumb}}"></image>
      <view>{{bestList.title}}</view>
    </view>
    
    </view>
</view>
